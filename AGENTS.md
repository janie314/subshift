
Read README.md for context on what the program should do.

Use Git liberally to test and roll back ideas.

Remember: a coding effort is not finished until the checks in `.github/workflows/ci.yml` pass. Also fix warnings given by `cargo build` with `cargo fix`.

Work hard and good luck.
